# lean-ctx

Use `lean-ctx -c "<command>"` for noisy shell commands such as git status,
diffs, tests, builds, npm installs, and file searches.

Use full file reads only when editing or validating exact content.
